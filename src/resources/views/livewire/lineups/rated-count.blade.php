<div class="px-2 font-black text-center text-gray-300 rounded-xl
    {{ $allRated ? 'bg-amber-600' : 'bg-gray-700' }}">
    <p class="text-xs">Rated</p>
    <p class="text-sm">{{ $ratedCount }} / {{ $playerCount }}</p>
</div>