<div class="px-2 font-black text-center text-gray-300 rounded-xl
    {{ $allEvaluated ? 'bg-amber-600' : 'bg-gray-700' }}">
    <p class="text-xs">Evaluated</p>
    <p class="text-sm">{{ $evaluatedCount }} / {{ $playerCount }}</p>
</div>